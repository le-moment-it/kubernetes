apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: ${name}
  namespace: ${namespace}
spec:
  acme:
    email: ${email}
    server: ${server}
    privateKeySecretRef:
      name: ${private_key_secret_name}
    solvers:
      - http01:
          ingress:
            class: nginx
